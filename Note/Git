今後の課題

	作成したローカルリポジトリをリモートにPushしたい
	リモートリポジトリをGitHub以外、具体的には自分の所有するサーバーにしたい	

git remote add origin <Repository Path>
	リモートリポジトリの追加
	事前にGitHubなど対象のでからのリポジトリの作成の必要あり？

git push origin master
	branch を指定してリモートへPut


config 
	各種設定を行う
	--global
		user.name
		user.ename
		core.editor
		core.pager
			PowerShell使うなら`less`はないので`more`を使用する
		core.quotepath
			日本語使うなら"false"を設定
		

init
	ローカルリポジトリを作成する

add
	ステージングエリアに変更を登録する

commit
	コミットを作成する

rm
	Git管理下のファイルやディレクトリを削除する

checkout
	ワークツリーの変更を取り消す

reset
	ステージングエリアに追加した変更をあワークツリーへ戻す

status
	ローカルリポジトリの状態を確認する

diff
	各エリアの差分を確認する

log
	コミットの履歴を確認する

ssh -T git@github.com
	GitHubへの接続テスト
	SSHキーをサイトに登録した後に行う

